import { SetRequired } from 'type-fest';
import { Props } from '../dist/types';
export declare const SLUG = "react-dropdown";
export declare const defaultProps: SetRequired<Omit<Props, 'options'>, 'closeOnSelect' | 'comparatorFn' | 'direction' | 'menuPosition' | 'values'>;
export declare const styledOptions: {
    shouldForwardProp: (prop: string) => boolean;
};

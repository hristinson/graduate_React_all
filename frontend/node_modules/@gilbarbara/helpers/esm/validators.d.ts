/**
 * Check if CPF is valid
 */
import { ValidatePasswordOptions } from './types';
export declare function isValidCPF(value: string): boolean;
/**
 * Check if email is valid
 */
export declare function isValidEmail(value: string): boolean;
/**
 * Validate password length and required characters
 * @throws
 */
export declare function validatePassword(password: string, options?: ValidatePasswordOptions): boolean;

/// <reference types="react" />
import { StringOrNumber } from '@gilbarbara/types';
import { ComponentProps, Direction, Sizes, Spacing, StyledProps, WithAlign, WithChildrenOptional, WithColor, WithMargin, WithTextSize } from './types';
export interface DividerKnownProps extends StyledProps, WithAlign, WithChildrenOptional, WithColor, WithMargin, WithTextSize {
    /** @default sm */
    borderSize?: Sizes;
    /** @default solid */
    borderStyle?: 'solid' | 'dashed' | 'dotted';
    /** @default horizontal */
    direction?: Direction;
    /**
     * The distance between the text and borders
     *
     * @default xs
     */
    gap?: Spacing;
    /**
     * The width (for horizontal) or height (for vertical)
     *
     * @default 100%
     */
    length?: StringOrNumber;
}
export declare type DividerProps = ComponentProps<HTMLDivElement, DividerKnownProps>;
export declare function Divider(props: DividerProps): JSX.Element;
export declare namespace Divider {
    var defaultProps: {
        align: string;
        borderSize: string;
        borderStyle: string;
        direction: string;
        gap: string;
        length: string;
        shade: string;
        size: string;
        variant: string;
    };
}

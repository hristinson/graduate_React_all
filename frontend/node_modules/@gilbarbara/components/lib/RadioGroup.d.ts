/// <reference types="react" />
import { StringOrNumber } from '@gilbarbara/types';
import { RadioProps } from './CheckboxAndRadio';
import { RadioItem, WithComponentSize } from './types';
export interface RadioGroupProps extends WithComponentSize, Omit<RadioProps, 'align' | 'checked' | 'defaultChecked' | 'label'> {
    defaultValue?: StringOrNumber;
    inline?: boolean;
    items: RadioItem[];
}
export declare function RadioGroup(props: RadioGroupProps): JSX.Element | null;
export declare namespace RadioGroup {
    var defaultProps: {
        disabled: boolean;
        inline: boolean;
        size: string;
    };
}

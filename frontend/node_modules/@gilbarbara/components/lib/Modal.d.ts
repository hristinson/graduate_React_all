import { CSSProperties, ReactNode } from 'react';
import { StringOrNumber } from '@gilbarbara/types';
import { StandardLonghandProperties } from 'csstype';
import { PortalProps } from './Portal';
import { StyledProps, WithBorder, WithPadding, WithRadius, WithShadow } from './types';
export interface ModalProps extends StyledProps, WithBorder, WithPadding, WithRadius, WithShadow, Omit<PortalProps, 'isActive' | 'showCloseButton'> {
    hideCloseButton?: boolean;
    isActive: boolean;
    maxHeight?: StandardLonghandProperties['maxHeight'] | number;
    maxWidth?: StandardLonghandProperties['maxWidth'] | number;
    style?: CSSProperties;
    title?: ReactNode;
    width?: StringOrNumber;
}
export declare function Modal(props: ModalProps): JSX.Element;
export declare namespace Modal {
    var defaultProps: {
        closeOnClickOverlay: boolean;
        closeOnEsc: boolean;
        hideCloseButton: boolean;
        hideOverlay: boolean;
        maxHeight: string;
        maxWidth: string;
        padding: string;
        radius: string;
        shadow: string;
        zIndex: number;
    };
}

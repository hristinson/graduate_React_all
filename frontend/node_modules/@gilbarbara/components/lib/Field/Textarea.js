"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const jsx_runtime_1 = require("react/jsx-runtime");
const react_1 = require("react");
const utils_1 = require("./utils");
const Textarea_1 = require("../Textarea");
function FieldTextarea(props) {
    const { id, isDirty, name, onBlur, onChange, onFocus, registration, setStatus, skipValidation } = props;
    const handleBlur = (0, react_1.useCallback)((event) => {
        setStatus({ isActive: false, isDirty });
        registration.onBlur(event);
        if (onBlur) {
            onBlur(event);
        }
    }, [isDirty, onBlur, registration, setStatus]);
    const handleFocus = (0, react_1.useCallback)((event) => {
        setStatus({ isActive: true });
        if (onFocus) {
            onFocus(event);
        }
    }, [onFocus, setStatus]);
    const handleChange = (0, react_1.useCallback)((event) => {
        registration.onChange(event);
        if (onChange) {
            onChange(event);
        }
    }, [onChange, registration]);
    const input = {
        ...(0, utils_1.getInputParameters)(props, 'isDirty', 'registration', 'setStatus'),
        id: id || name,
        onBlur: handleBlur,
        onChange: handleChange,
        onFocus: handleFocus,
    };
    return (0, jsx_runtime_1.jsx)(Textarea_1.Textarea, { ...registration, ...input, suffixSpacing: !skipValidation });
}
exports.default = FieldTextarea;
//# sourceMappingURL=Textarea.js.map
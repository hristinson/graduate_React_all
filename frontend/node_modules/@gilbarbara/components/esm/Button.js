import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { forwardRef } from 'react';
import { css } from '@emotion/react';
import styled from '@emotion/styled';
import { Icon } from './Icon';
import { getColorVariant, getTheme, px } from './modules/helpers';
import { appearanceStyles, backgroundStyles, baseStyles, getStyledOptions, paddingStyles, } from './modules/system';
export const StyledButton = styled('button', getStyledOptions())(props => {
    const { block, busy, shade, shape, size = 'md', variant = 'primary', wide } = props;
    const { button, grayLighter, grayMid, radius, spacing, variants } = getTheme(props);
    const { borderRadius, fontSize, fontWeight, height, lineHeight, padding } = button;
    let buttonPadding = `${padding[size][0]} ${wide ? px(parseInt(padding[size][1], 10) * 2) : padding[size][1]}`;
    let selectedRadius = borderRadius[size];
    if (shape) {
        buttonPadding = spacing.xxs;
        switch (shape) {
            case 'square': {
                selectedRadius = `0`;
                break;
            }
            case 'circle': {
                selectedRadius = radius.round;
                break;
            }
        }
    }
    return css `
    ${appearanceStyles};
    ${baseStyles(props)};
    align-items: center;
    border-radius: ${selectedRadius};
    box-shadow: none;
    cursor: pointer;
    display: inline-flex;
    font-size: ${fontSize[size]};
    font-weight: ${fontWeight};
    min-height: ${height[size]};
    min-width: ${height[size]};
    justify-content: center;
    line-height: ${lineHeight[size]};
    overflow: hidden;
    padding: ${buttonPadding};
    position: relative;
    transition: background-color 0.6s, border-color 0.6s;
    width: ${block ? '100%' : 'auto'};
    ${backgroundStyles(props)};
    ${paddingStyles(props)}

    &:disabled {
      background-color: ${grayLighter};
      border-color: ${grayLighter};
      color: ${grayMid};
      pointer-events: none;
    }

    &:focus {
      outline-color: ${getColorVariant(variant, shade, variants).bg};
    }

    ${!!busy &&
        css `
      pointer-events: none;
    `};
  `;
});
export const Button = forwardRef((props, ref) => {
    const { busy, children, shape, size = 'md' } = props;
    const { button: { fontSize }, } = getTheme(props);
    const content = {
        children,
        icon: !!busy && _jsx(Icon, { ml: "sm", name: "spinner", size: parseInt(fontSize[size], 10) + 4, spin: true }),
    };
    if (shape && busy) {
        content.children = _jsx(Icon, { name: "spinner", size: parseInt(fontSize[size], 10) + 4, spin: true });
        content.icon = '';
    }
    return (_jsxs(StyledButton, { ref: ref, "data-component-name": "Button", ...props, children: [content.children, content.icon] }));
});
Button.defaultProps = {
    block: false,
    busy: false,
    disabled: false,
    invert: false,
    shade: 'mid',
    size: 'md',
    transparent: false,
    type: 'button',
    variant: 'primary',
    wide: false,
};
//# sourceMappingURL=Button.js.map
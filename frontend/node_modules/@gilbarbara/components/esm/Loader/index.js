import { jsx as _jsx } from "react/jsx-runtime";
import { useTheme } from '@emotion/react';
import LoaderGrow from './Grow';
import LoaderPill from './Pill';
import LoaderPride from './Pride';
import LoaderPulse from './Pulse';
import LoaderRotate from './Rotate';
import { getTheme } from '../modules/helpers';
export function Loader(props) {
    const { type, ...rest } = props;
    const theme = getTheme({ theme: useTheme() });
    switch (type) {
        case 'grow': {
            return _jsx(LoaderGrow, { theme: theme, ...rest });
        }
        case 'pride': {
            return _jsx(LoaderPride, { theme: theme, ...rest });
        }
        case 'pulse': {
            return _jsx(LoaderPulse, { theme: theme, ...rest });
        }
        case 'rotate': {
            return _jsx(LoaderRotate, { theme: theme, ...rest });
        }
        case 'pill':
        default: {
            return _jsx(LoaderPill, { theme: theme, ...rest });
        }
    }
}
Loader.defaultProps = {
    block: false,
    shade: 'mid',
    type: 'pill',
    variant: 'primary',
};
//# sourceMappingURL=index.js.map
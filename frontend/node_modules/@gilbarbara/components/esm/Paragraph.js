import { jsx as _jsx } from "react/jsx-runtime";
import { forwardRef, isValidElement } from 'react';
import { css } from '@emotion/react';
import styled from '@emotion/styled';
import { getTheme, px } from './modules/helpers';
import { alignStyles, baseStyles, colorStyles, getStyledOptions, marginStyles, } from './modules/system';
import { Text } from './Text';
export const StyledParagraph = styled('p', getStyledOptions())(props => {
    const { spacing } = getTheme(props);
    return css `
    ${baseStyles(props)};
    margin-bottom: 0;
    margin-top: 0;
    ${alignStyles(props)};
    ${colorStyles(props)};
    ${marginStyles(props)};

    & + & {
      margin-top: ${px(spacing.sm)};
    }
  `;
});
export const Paragraph = forwardRef((props, ref) => {
    const { children, ...rest } = props;
    return (_jsx(StyledParagraph, { ref: ref, "data-component-name": "Paragraph", ...props, children: isValidElement(children) ? children : _jsx(Text, { ...rest, children: children }) }));
});
Paragraph.defaultProps = {
    bold: false,
    size: 'regular',
};
//# sourceMappingURL=Paragraph.js.map
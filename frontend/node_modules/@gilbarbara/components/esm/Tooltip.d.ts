import { CSSProperties, ReactNode } from 'react';
import { Sizes, WithChildren, WithColor, WithOpen, WithTextOptions } from './types';
interface SharedProps {
    /** @default middle */
    align: 'start' | 'middle' | 'end';
    /** @default bottom */
    position: 'bottom' | 'left' | 'right' | 'top';
    /** Content wrapping */
    wrap?: Sizes;
}
export interface TooltipProps extends Partial<SharedProps>, WithChildren, WithColor, WithOpen, WithTextOptions {
    content: ReactNode;
    style?: CSSProperties;
}
export declare function Tooltip(props: TooltipProps): JSX.Element;
export declare namespace Tooltip {
    var defaultProps: {
        align: string;
        bold: boolean;
        position: string;
        shade: string;
        size: string;
        variant: string;
    };
}
export {};

/// <reference types="react" />
import { StringOrNumber } from '@gilbarbara/types';
import { StyledProps, WithChildren } from './types';
export interface TruncateProps extends StyledProps, WithChildren {
    /** @default 2 */
    lines?: number;
    maxWidth?: StringOrNumber;
}
export declare function Truncate(props: TruncateProps): JSX.Element;
export declare namespace Truncate {
    var defaultProps: {
        lines: number;
    };
}

import { jsx as _jsx } from "react/jsx-runtime";
import styled from '@emotion/styled';
import { ButtonBase } from '../ButtonBase';
const StyledDropdownAdd = styled.div `
  width: 100%;

  button {
    display: flex;
    justify-content: center;
    padding: 8px;
    width: 100%;
  }
`;
function DropdownAdd(props) {
    const { methods: { getLabels, setStatus }, onCreate, shade, state: { search }, variant = 'primary', } = props;
    const handleClick = () => {
        if (onCreate) {
            onCreate(search, () => setStatus('close'));
        }
    };
    return (_jsx(StyledDropdownAdd, { "data-component-name": "DropdownAdd", children: _jsx(ButtonBase, { onClick: handleClick, shade: shade, variant: variant, children: getLabels().create.replace(/{search}/, `"${search}"`) }) }));
}
export default DropdownAdd;
//# sourceMappingURL=Add.js.map
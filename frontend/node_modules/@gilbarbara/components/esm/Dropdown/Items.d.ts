/// <reference types="react" />
import { ComponentProps } from '@gilbarbara/react-dropdown';
import { DropdownProps, WithColor } from '../types';
interface DropdownOptionsProps extends WithColor, ComponentProps, Pick<DropdownProps, 'allowCreate' | 'onCreate' | 'onSearch'> {
}
declare function DropdownOptions({ methods, onCreate, onSearch, props, shade, state, variant, }: DropdownOptionsProps): JSX.Element;
export default DropdownOptions;

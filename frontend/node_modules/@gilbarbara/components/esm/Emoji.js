import { jsx as _jsx } from "react/jsx-runtime";
import { forwardRef } from 'react';
import { css } from '@emotion/react';
import styled from '@emotion/styled';
import { px } from './modules/helpers';
import { baseStyles, getStyledOptions } from './modules/system';
export const StyledEmoji = styled('span', getStyledOptions())(props => {
    const { size } = props;
    return css `
    ${baseStyles(props)};
    display: inline-flex;
    line-height: 1;

    ${size &&
        css `
      font-size: ${px(size)};
      height: ${px(size)};
      width: ${px(size)};
    `};
  `;
});
export const Emoji = forwardRef((props, ref) => {
    const { label, size, symbol } = props;
    return (_jsx(StyledEmoji, { ref: ref, "aria-hidden": label ? 'false' : 'true', "aria-label": label || '', "data-component-name": "Emoji", size: size, children: symbol }));
});
//# sourceMappingURL=Emoji.js.map